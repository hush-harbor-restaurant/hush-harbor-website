{
  "name": "hush-harbor",
  "private": true,
  "scripts": {
    "studio": "cd cms && sanity build && cp -r dist ../frontend/dist/studio",
    "build": "npm run build --workspace frontend"
  },
  "workspaces": [
    "frontend",
    "cms"
  ]
}
